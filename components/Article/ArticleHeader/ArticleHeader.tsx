import Header from '../../UI/Header/Header'
interface Props {}

function ArticleHeader(props: Props) {
  const {} = props

  return (
      <Header>
        <h2>Main</h2>
        <i>door</i>
      </Header>
  )
}

export default ArticleHeader
